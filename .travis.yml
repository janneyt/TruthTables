language: python
python:

  - "3.8"

# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - python -m unittest tests.py truthTables.py
CODECOV_TOKEN="dad0011f-0bf6-461d-9b13-6c75832ea68a"
